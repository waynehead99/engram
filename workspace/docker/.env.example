# ── Engram Docker Deployment ───────────────────────────────────────────
# Copy this file to .env and fill in your values.
# NEVER commit .env — it's in .gitignore.
#
# NOTE: Slack, Google, and Ollama credentials are NOT set here.
# They live in the config directory's .env file (OPENCLAW_CONFIG_DIR/.env)
# which OpenClaw loads automatically via dotenv at startup.

# ── Gateway Auth (required) ───────────────────────────────────────────
# Generate with: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=

# ── Paths (required) ─────────────────────────────────────────────────
# Host paths to mount into the container
# The config dir must contain: openclaw.json, .env (with Slack/Google/Ollama keys)
OPENCLAW_CONFIG_DIR=/opt/engram/config
OPENCLAW_WORKSPACE_DIR=/opt/engram/workspace

# ── Gateway Port ──────────────────────────────────────────────────────
# Port exposed on the LXC host (maps to container port 18789)
OPENCLAW_GATEWAY_PORT=18789

# ── Optional ──────────────────────────────────────────────────────────
# Claude.ai session key for usage dashboard (sk-ant-* from browser cookies)
# Not required — only shows rate limit % in the Control UI
# CLAUDE_AI_SESSION_KEY=
