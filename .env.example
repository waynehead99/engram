# ── Engram Docker Deployment ───────────────────────────────────────────
# Copy this file to .env and fill in your values.
# NEVER commit .env — it's in .gitignore.
#
# There are TWO .env files:
#   1. THIS FILE (repo root) — Docker Compose variables (gateway token, paths)
#   2. CONFIG DIR .env       — App secrets (Slack, Google, Notion tokens)
#
# See README.md for detailed setup instructions.

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# REQUIRED — Docker Compose Variables
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Gateway authentication token (protects the web UI)
# Generate with: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=

# Claude Code CLI OAuth token (powers the AI backend)
# Generate on your Mac with: claude setup-token
# Token starts with sk-ant-oat01-... and is valid for ~1 year
CLAUDE_CODE_OAUTH_TOKEN=

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# REQUIRED — Host Paths
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Path on your server where Engram config lives
# Must contain: openclaw.json and .env (with Slack/Google/Notion keys)
OPENCLAW_CONFIG_DIR=/opt/engram/config

# Path on your server where the workspace lives
# Contains: personality files, skills, memory
OPENCLAW_WORKSPACE_DIR=/opt/engram/workspace

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# OPTIONAL — Customization
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Port exposed on the host (default: 18789)
# OPENCLAW_GATEWAY_PORT=18789
