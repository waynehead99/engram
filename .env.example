# ── Engram Docker Deployment ───────────────────────────────────────────
# Copy this file to .env and fill in your values.
# NEVER commit .env — it's in .gitignore.

# ── Gateway Auth ──────────────────────────────────────────────────────
# Generate with: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=

# ── Claude CLI ────────────────────────────────────────────────────────
# Get from: claude auth status (or browser session cookie)
# Required for Claude Code CLI backend inside the container
CLAUDE_AI_SESSION_KEY=

# ── Paths ─────────────────────────────────────────────────────────────
# Host paths to mount into the container
# Absolute paths on the LXC host (adjust to your setup)
OPENCLAW_CONFIG_DIR=/opt/engram/config
OPENCLAW_WORKSPACE_DIR=/opt/engram/workspace

# ── Gateway Port ──────────────────────────────────────────────────────
# Port exposed on the LXC host (maps to container port 18789)
OPENCLAW_GATEWAY_PORT=18789
